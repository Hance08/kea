// Placeholder — replaced by the full detail page in Task 14.
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/transactions/$id')({
  component: () => <div>Transaction Detail</div>,
});
